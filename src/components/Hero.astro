---
interface Props {
    sampleSize: number;
    modelConfidence: number;
}
const { sampleSize, modelConfidence } = Astro.props;
---

<header class="pt-20 pb-16 px-6 bg-white">
    <div class="max-w-3xl mx-auto text-center">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 leading-tight mb-6">
            Predictive Valuation Engine.
        </h1>
        <p class="text-xl text-gray-500 font-light leading-relaxed">
            Townhouse value is driven by more than just square footage. 
            Our <strong>Multivariate OLS Model</strong> isolates the value of specific features like private garages, end units, and location premiums.
        </p>
        <div class="mt-6 inline-flex items-center gap-2 px-4 py-1.5 bg-blue-50 text-blue-700 text-xs font-medium rounded-full border border-blue-100">
            <span>N = {sampleSize}</span>
            <span class="text-blue-300">|</span>
            <span>R² = {(modelConfidence * 100).toFixed(1)}%</span>
            <span class="text-blue-300">|</span>
            <span>Variable Coefficients Active</span>
        </div>
    </div>
</header>
